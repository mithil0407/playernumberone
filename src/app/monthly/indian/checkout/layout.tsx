import { noIndexMetadata } from "@/lib/seo";

export const metadata = noIndexMetadata;

export default function MonthlyIndianCheckoutLayout({ children }: { children: React.ReactNode }) {
  return children;
}
