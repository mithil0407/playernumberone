import { noIndexMetadata } from "@/lib/seo";

export const metadata = noIndexMetadata;

export default function GlobeThankYouLayout({ children }: { children: React.ReactNode }) {
  return children;
}
