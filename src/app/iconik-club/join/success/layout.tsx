import { noIndexMetadata } from "@/lib/seo";

export const metadata = noIndexMetadata;

export default function ClubJoinSuccessLayout({ children }: { children: React.ReactNode }) {
  return children;
}
